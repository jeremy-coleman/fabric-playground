// NOTE: this is the global app config type definition
declare var AppConfig : {
    production?: boolean;
    publicPath?: string;
    buildVersion?: string;
    buildDate?: string;
    env?: any;
};